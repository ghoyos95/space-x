package com.spacex.restapi.services;

import com.spacex.restapi.models.TrelloCard;


public class TrelloService {

    public String createCard(TrelloCard trelloCard) throws Exception {
        /*
        HttpHeaders headers = new HttpHeaders();
        RestTemplate restTemplate = new RestTemplate();
        ResponseEntity<String> response;
       
        buildTrelloApiUrl(trelloCard);
        buildRequestBody(trelloCard);

        try {
            response = restTemplate.postForEntity(apiUrl, requestEntity, String.class);
        } catch (Exception e) {
            throw new Exception("Failed to create card: ");
        }
        
        */
         
        return "";
    }
}
